export interface ThemeColors {
    background: string;
    color: string;
    link: string;
    linkHover: string;
}

export interface AccordionTheme {
    root: any;
    title: any;
    body: any;
}
